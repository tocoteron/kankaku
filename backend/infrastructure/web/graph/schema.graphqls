# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: ID!
  name: String!
  posts: [Post!]!
}

type Post {
  id: ID!
  content: String!
}

type Query {
  me: User!
}

type Mutation {
  post(content: String!): Post!
}
